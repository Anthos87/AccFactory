# Server

Backend per l'applicazione E-commerce.
Tecnologia: Python/Node.js.

- Lavorare esclusivamente all'interno di questa cartella.
- Sviluppare gli endpoint in conformità al contratto API `api_contract.md`.
- Prima di ogni modifica assicurarsi di aver fatto `git pull origin main`.
